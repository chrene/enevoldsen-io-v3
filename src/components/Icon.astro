---
export interface Props {
  name: string;
  class: string;
}

const { name, ...rest } = Astro.props as Props;
const { default: innerHTML } = await import(`../assets/svg/${name}.svg?raw`);
---

<div {...rest}>
  <Fragment set:html={innerHTML} />
</div>
